<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:gravity="left|top"	
    android:background="@color/gray_light"
    >

    <RelativeLayout
        android:layout_below="@id/title_view"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        >
        
        <TextView android:id="@+id/comment_txv"
	        android:layout_width="fill_parent"
	        android:layout_height="wrap_content" 
	        android:layout_alignParentTop="true"
	        android:paddingLeft="16dp"
	        android:paddingTop="4dp"
	        android:paddingRight="4dp"
	        android:paddingBottom="4dp"
	        android:textSize="14sp"
	        android:textColor="@color/black_light"
	        android:singleLine="false"
	        android:maxLines="2"
            android:background="@color/gray_light"/>
        
        <ImageView 
	        android:id="@+id/shadow2"
            android:layout_below="@id/comment_txv"
	        android:layout_width="fill_parent"
	        android:layout_height="wrap_content"
	        android:background="@color/white"
	        android:scaleType="fitXY"
	        android:src="@drawable/bg_line_split" />
        
        <com.tigerknows.widget.SpringbackListView android:id="@+id/result_lsv"
            android:layout_below="@id/shadow2"
            android:layout_width="fill_parent" 
            android:layout_height="fill_parent"
            android:divider="@drawable/bg_line_split"
            android:scrollingCache="false"
            android:fadingEdge="none"
            android:drawSelectorOnTop="false"
            android:background="@color/gray_dark" />
             
    </RelativeLayout>
</RelativeLayout>
