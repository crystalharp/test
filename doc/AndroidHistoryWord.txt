1）用户输入历史获取：
a.应用启动时，获取已有历史；
b.点击要搜索的词时；最多50个；
c.分为poi搜索、交通两类；
2）历史显示：
a.第一次进入搜索/交通对话框没有输入时，显示最多10个历史词；
b.输入时，最多显示3个历史词，与联想词去重合并显示；
3）保存：
a.退出应用时；
b.运行中以队列保存；
4）显示/隐藏时机:
a.交通查询中：初始隐藏/显示皆可，click起点终点切换按钮时隐藏，onTouch起点终点输入框时显示；
b.搜索：初始显示，click分类listview设置text前隐藏，之后显示；